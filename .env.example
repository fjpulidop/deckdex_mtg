# DeckDex MTG - Environment Variables Example
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Google Sheets API credentials
# Path to your Google service account JSON credentials file
GOOGLE_API_CREDENTIALS=/path/to/your/credentials.json

# OpenAI API key (optional, for card strategy and tier enrichment)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI model selection (optional, defaults to gpt-3.5-turbo)
OPENAI_MODEL=gpt-3.5-turbo

# PostgreSQL (required for collection storage when using Postgres as source of truth)
# Example: postgresql://user:password@localhost:5432/deckdex
DATABASE_URL=postgresql://localhost:5432/deckdex

# Configuration profile (optional, defaults to 'default')
# Options: default, development, production
DECKDEX_PROFILE=default

# Google OAuth 2.0 credentials (required for authentication)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# Client ID for your OAuth 2.0 application (Web application)
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id-here

# Client secret for your OAuth 2.0 application
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret-here

# JWT secret key for signing authentication tokens (required)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-jwt-secret-key-here-must-be-at-least-32-characters

# Admin user email (optional)
# Set to the email of the user who should have admin access
# When set, this user will see the Admin dashboard and can trigger catalog sync
DECKDEX_ADMIN_EMAIL=

# Backend API settings (optional)
# API_HOST=0.0.0.0
# API_PORT=8000
# API_RELOAD=true
